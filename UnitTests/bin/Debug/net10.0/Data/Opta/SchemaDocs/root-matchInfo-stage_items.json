{
  "Nesting": "root-matchInfo-stage_items",
  "Description": "Required. Root element of the fields related to the stage. Value (XML only): String giving the stage name",
  "Required": true,
  "attributes": [
    {
      "Attribute": "id",
      "Description": "Required. Unique ID (UUID) of the stage",
      "Required": true,
      "Type": "String (Alphanumeric - UUID)"
    },
    {
      "Attribute": "ocId",
      "Description": "Optional. Unique Opta Core stage ID. Returned only if the feed is queried by Opta Core legacy ID; the attribute is not present otherwise. Empty string if no such value exists.",
      "Required": false,
      "Type": "String"
    },
    {
      "Attribute": "opId",
      "Description": "Optional. Unique Opta stage ID. Returned only if the feed is queried by Opta legacy ID; the attribute is not present otherwise. Empty string if no such value exists.",
      "Required": false,
      "Type": "String"
    },
    {
      "Attribute": "formatId",
      "Description": "Required. Unique ID (UUID) of the stage format. This is an identifier for a specific type of stage - despite being played in different seasons, a team (contestant) can have the same format ID but a different stage ID (the stage ID is linked with stage and season).",
      "Required": true,
      "Type": "String (Alphanumeric - UUID)"
    },
    {
      "Attribute": "startDate",
      "Description": "Required. The start date (UTC) of the stage",
      "Required": true,
      "Type": "Date - in the format: YYYY-MM-DDZ"
    },
    {
      "Attribute": "endDate",
      "Description": "Required. The end date (UTC) of the stage",
      "Required": true,
      "Type": "Date - in the format: YYYY-MM-DDZ"
    },
    {
      "Attribute": "name (JSON only)",
      "Description": "Required. Name of the stage",
      "Required": true,
      "Type": "String"
    }
  ]
}